<?xml version="1.0" encoding="UTF-8"?>
<web-app version="3.0" xmlns="http://java.sun.com/xml/ns/javaee"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://java.sun.com/xml/ns/javaee    http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd">

  <!-- Fetch And Lock Handler (long polling): -->

  <!-- Unique Worker Request (default value: false)
  <context-param>
    <param-name>fetch-and-lock-unique-worker-request</param-name>
    <param-value>true</param-value>
  </context-param>
  -->

  <!-- Queue capacity (default value: 200)
  <context-param>
    <param-name>fetch-and-lock-queue-capacity</param-name>
    <param-value>250</param-value>
  </context-param>
  -->

  <!-- rest bootstrap listener -->
  <listener>
    <listener-class>org.cadenzaflow.bpm.engine.rest.impl.web.bootstrap.RestContainerBootstrap</listener-class>
  </listener>

  <listener>
    <listener-class>org.cadenzaflow.bpm.engine.rest.impl.FetchAndLockContextListener</listener-class>
  </listener>

  <filter>
    <filter-name>EmptyBodyFilter</filter-name>
    <filter-class>org.cadenzaflow.bpm.engine.rest.filter.EmptyBodyFilter</filter-class>
    <async-supported>true</async-supported>
  </filter>
  <filter-mapping>
    <filter-name>EmptyBodyFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <filter>
    <filter-name>CacheControlFilter</filter-name>
    <filter-class>org.cadenzaflow.bpm.engine.rest.filter.CacheControlFilter</filter-class>
    <async-supported>true</async-supported>
  </filter>
  <filter-mapping>
    <filter-name>CacheControlFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <!-- Http Basic Authentication Filter -->
  <!-- <filter>
    <filter-name>camunda-auth</filter-name>
    <filter-class>
      org.cadenzaflow.bpm.engine.rest.security.auth.ProcessEngineAuthenticationFilter
    </filter-class>
	<async-supported>true</async-supported>
    <init-param>
      <param-name>authentication-provider</param-name>
      <param-value>org.cadenzaflow.bpm.engine.rest.security.auth.impl.HttpBasicAuthenticationProvider</param-value>
    </init-param>
    <init-param>
	    <param-name>rest-url-pattern-prefix</param-name>
	    <param-value></param-value>
	  </init-param>
  </filter>

  <filter-mapping>
    <filter-name>camunda-auth</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping> -->

  <!-- Keycloak / OIDC Bearer Authentication Filter (alternative to Http Basic above).
       Validates "Authorization: Bearer <JWT>" access tokens offline against the
       issuer's JWKS - no call to the identity provider per request. Use
       CompositeAuthenticationProvider to accept BOTH Bearer and Basic during a
       migration, or KeycloakBearerTokenAuthenticationProvider for Bearer-only.
       Only keycloak.issuer-uri and keycloak.audience are required; see
       KeycloakProviderConfig for all parameters. Keycloak service-account tokens
       (grant_type=client_credentials) usually carry the client id in the azp
       claim, not aud - hence audience-mode=azp below. Prefer client_credentials
       for machine-to-machine calls; grant_type=password (ROPC) is a legacy
       convenience only. -->
  <!-- <filter>
    <filter-name>camunda-auth</filter-name>
    <filter-class>
      org.cadenzaflow.bpm.engine.rest.security.auth.ProcessEngineAuthenticationFilter
    </filter-class>
    <async-supported>true</async-supported>
    <init-param>
      <param-name>authentication-provider</param-name>
      <param-value>org.cadenzaflow.bpm.engine.rest.security.auth.impl.CompositeAuthenticationProvider</param-value>
    </init-param>
    <init-param>
      <param-name>keycloak.issuer-uri</param-name>
      <param-value>https://keycloak.example.com/realms/my-realm</param-value>
    </init-param>
    <init-param>
      <param-name>keycloak.audience</param-name>
      <param-value>my-rest-client</param-value>
    </init-param>
    <init-param>
      <param-name>keycloak.audience-mode</param-name>
      <param-value>azp</param-value>
    </init-param>
  </filter>

  <filter-mapping>
    <filter-name>camunda-auth</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping> -->

  <servlet>
    <servlet-name>Resteasy</servlet-name>
    <servlet-class>org.jboss.resteasy.plugins.server.servlet.HttpServlet30Dispatcher</servlet-class>
    <init-param>
      <param-name>javax.ws.rs.Application</param-name>
      <param-value>org.cadenzaflow.bpm.engine.rest.impl.application.DefaultApplication</param-value>
    </init-param>
    <async-supported>true</async-supported>
  </servlet>

  <servlet-mapping>
    <servlet-name>Resteasy</servlet-name>
    <url-pattern>/*</url-pattern>
  </servlet-mapping>

</web-app>