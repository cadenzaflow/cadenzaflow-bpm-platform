<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- HEADS-UP: Do not use camunda-parent as parent,
  it would introduce a cyclic dependency to this bom -->
  <parent>
    <groupId>org.cadenzaflow.bpm</groupId>
    <artifactId>cadenzaflow-root</artifactId>
    <version>1.2.0</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <groupId>org.cadenzaflow.connect</groupId>
  <artifactId>cadenzaflow-connect-bom</artifactId>
  <version>1.2.0</version>
  <name>CadenzaFlow Platform - connect - bom</name>
  <packaging>pom</packaging>

  <dependencyManagement>
    <dependencies>

      <dependency>
        <groupId>org.cadenzaflow.connect</groupId>
        <artifactId>cadenzaflow-connect-core</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.cadenzaflow.connect</groupId>
        <artifactId>cadenzaflow-connect-http-client</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.cadenzaflow.connect</groupId>
        <artifactId>cadenzaflow-connect-soap-http-client</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.cadenzaflow.connect</groupId>
        <artifactId>cadenzaflow-connect-connectors-all</artifactId>
        <version>${project.version}</version>
      </dependency>

    </dependencies>
  </dependencyManagement>

  <scm>
    <url>https://github.com/cadenzaflow/cadenzaflow-bpm-platform</url>
    <connection>scm:git:git@github.com:cadenzaflow/cadenzaflow-bpm-platform.git</connection>
    <developerConnection>scm:git:git@github.com:cadenzaflow/cadenzaflow-bpm-platform.git</developerConnection>
    <tag>1.1.0</tag>
  </scm>

</project>
