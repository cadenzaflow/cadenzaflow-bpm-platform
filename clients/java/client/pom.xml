<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <name>CadenzaFlow Platform - Java External Task Client - CLIENT</name>

  <artifactId>cadenzaflow-external-task-client</artifactId>

  <packaging>jar</packaging>

  <parent>
    <groupId>org.cadenzaflow.bpm</groupId>
    <artifactId>cadenzaflow-external-task-client-root</artifactId>
    <version>1.2.0</version>
  </parent>

  <properties>
    <engine.runtime>${project.build.directory}/cadenzaflow-tomcat</engine.runtime>
    <tomcat.runtime>${engine.runtime}/server/apache-tomcat-${version.tomcat}</tomcat.runtime>

    <!-- it.http.port, it.ajp.port and it.rmi.port are reserved at build time by
         build-helper:reserve-network-port so that concurrent builds on the same
         machine cannot collide on fixed ports. They must not get default values
         here: a value set in <properties> would be interpolated into the plugin
         configurations when the POM is loaded, before the ports are reserved. -->

    <cargo.timeout>240000</cargo.timeout>
    <cargo.deploy.timeout>60000</cargo.deploy.timeout>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.cadenzaflow.bpm</groupId>
        <artifactId>cadenzaflow-core-internal-dependencies</artifactId>
        <version>${project.version}</version>
        <scope>import</scope>
        <type>pom</type>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${version.jackson}</version>
        <scope>import</scope>
        <type>pom</type>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>

    <!-- mandatory dependencies -->
    <dependency>
      <groupId>org.cadenzaflow.commons</groupId>
      <artifactId>cadenzaflow-commons-logging</artifactId>
    </dependency>

    <dependency>
      <groupId>org.cadenzaflow.commons</groupId>
      <artifactId>cadenzaflow-commons-typed-values</artifactId>
    </dependency>

    <dependency>
      <groupId>org.apache.httpcomponents.client5</groupId>
      <artifactId>httpclient5</artifactId>
      <version>${version.httpclient5}</version>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>

    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-annotations</artifactId>
    </dependency>

    <!-- test dependencies -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>jcl-over-slf4j</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.cadenzaflow.bpm.model</groupId>
      <artifactId>cadenzaflow-bpmn-model</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.skyscreamer</groupId>
      <artifactId>jsonassert</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.cadenzaflow.spin</groupId>
      <artifactId>cadenzaflow-spin-dataformat-all</artifactId>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.cadenzaflow.bpm.qa</groupId>
      <artifactId>engine-variable-test</artifactId>
      <version>${project.version}</version>
      <classifier>classes</classifier>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.cadenzaflow.bpm.tomcat</groupId>
      <artifactId>cadenzaflow-tomcat-assembly</artifactId>
      <version>${project.version}</version>
      <type>tar.gz</type>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <artifactId>slf4j-api</artifactId>
          <groupId>org.slf4j</groupId>
        </exclusion>
      </exclusions>
    </dependency>

    <dependency>
      <groupId>org.cadenzaflow.bpm</groupId>
      <artifactId>cadenzaflow-engine-rest-jakarta</artifactId>
      <scope>test</scope>
      <type>war</type>
      <classifier>tomcat</classifier>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>org.cadenzaflow.bpm.qa</groupId>
      <artifactId>engine-variable-test</artifactId>
      <scope>test</scope>
      <type>war</type>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>com.sun.xml.bind</groupId>
      <artifactId>jaxb-impl</artifactId>
      <version>${version.xml.jaxb-impl4}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>

      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>reserve-it-ports</id>
            <phase>process-resources</phase>
            <goals>
              <goal>reserve-network-port</goal>
            </goals>
            <configuration>
              <portNames>
                <portName>it.http.port</portName>
                <portName>it.ajp.port</portName>
                <portName>it.rmi.port</portName>
              </portNames>
            </configuration>
          </execution>
          <execution>
            <id>add-it-source</id>
            <phase>generate-test-sources</phase>
            <goals>
              <goal>add-test-source</goal>
            </goals>
            <configuration>
              <sources>
                <source>src/it/java</source>
              </sources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <trimStackTrace>false</trimStackTrace>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <phase>generate-resources</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <skip>${skipTests}</skip>
              <artifactItems>
                <artifactItem>
                  <groupId>org.cadenzaflow.bpm.tomcat</groupId>
                  <artifactId>cadenzaflow-tomcat-assembly</artifactId>
                  <version>${project.version}</version>
                  <type>tar.gz</type>
                  <outputDirectory>${engine.runtime}</outputDirectory>
                  <overWrite>true</overWrite>
                </artifactItem>
              </artifactItems>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <artifactId>maven-failsafe-plugin</artifactId>
        <configuration>
          <redirectTestOutputToFile>true</redirectTestOutputToFile>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.codehaus.cargo</groupId>
        <artifactId>cargo-maven3-plugin</artifactId>
        <executions>
          <execution>
            <id>start-container</id>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>start</goal>
            </goals>
          </execution>
          <execution>
            <id>stop-container</id>
            <phase>post-integration-test</phase>
            <goals>
              <goal>stop</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <skip>${skipTests}</skip>
          <!-- Container configuration -->
          <container>
            <type>installed</type>
            <containerId>tomcat10x</containerId>
            <append>false</append>
            <home>${tomcat.runtime}</home>
            <timeout>${cargo.timeout}</timeout>
            <log>${tomcat.runtime}/logs/catalina.out</log>
          </container>
          <configuration>
            <type>standalone</type>
            <home>${project.build.directory}/cargo-config-tomcat</home>
            <properties>
              <cargo.servlet.port>${it.http.port}</cargo.servlet.port>
              <cargo.tomcat.ajp.port>${it.ajp.port}</cargo.tomcat.ajp.port>
              <cargo.rmi.port>${it.rmi.port}</cargo.rmi.port>
              <cargo.start.jvmargs>-Djava.security.egd=file:/dev/./urandom</cargo.start.jvmargs>
            </properties>
            <files>
              <copy>
                <configfile>true</configfile>
                <file>${project.basedir}/src/it/resources/tomcat/bpm-platform.xml</file>
                <tofile>conf/bpm-platform.xml</tofile>
                <overwrite>true</overwrite>
              </copy>
            </files>
          </configuration>
          <deployables>
            <deployable>
              <groupId>org.cadenzaflow.bpm</groupId>
              <artifactId>cadenzaflow-engine-rest-jakarta</artifactId>
              <type>war</type>
              <classifier>tomcat</classifier>
              <pingURL>
                http://localhost:${it.http.port}/engine-rest/engine/default/process-definition
              </pingURL>
              <pingTimeout>${cargo.deploy.timeout}</pingTimeout>
              <properties>
                <context>engine-rest</context>
              </properties>
            </deployable>
            <deployable>
              <groupId>org.cadenzaflow.bpm.qa</groupId>
              <artifactId>engine-variable-test</artifactId>
              <type>war</type>
            </deployable>
          </deployables>
          <wait>false</wait>
        </configuration>
      </plugin>

    </plugins>

    <testResources>
      <!-- integration-rules.properties must advertise the engine-rest URL on the
           dynamically reserved it.http.port; everything else is copied verbatim
           so that BPMN models and test files are never touched by filtering. -->
      <testResource>
        <directory>src/it/resources</directory>
        <filtering>true</filtering>
        <includes>
          <include>integration-rules.properties</include>
        </includes>
      </testResource>
      <testResource>
        <directory>src/it/resources</directory>
        <excludes>
          <exclude>integration-rules.properties</exclude>
        </excludes>
      </testResource>
      <testResource>
        <directory>src/test/resources</directory>
      </testResource>
    </testResources>

  </build>

</project>
